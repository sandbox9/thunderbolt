apply plugin: 'java'
apply plugin: 'war'

version = '1.0-SNAPSHOT'

war {
    baseName = "site"
}

repositories {
    mavenCentral()
}

dependencies {
    compile project (':commonlib')
    compile project (':model')
}